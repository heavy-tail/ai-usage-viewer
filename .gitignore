node_modules
dist
.runtime
data
*.tsbuildinfo
*.local
.DS_Store

# Personal local config — each user keeps their own (copy config.example.json)
config.json

# Local-only deploy notes
DEPLOY_PLAN.md

# Local AI-assistant notes — never publish
CLAUDE.md
